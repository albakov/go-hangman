# Игра Hangman написанная на Go

## Сборка игры
Команда для сборки:

`make build`

Или:

`go build cmd/main.go && mv main hangman`

## Запуск игры

`./hangman`

# Словарь
Файл со словами:

`config/app.toml`
